apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
configMapGenerator:
  - name: notify-config
    envs:
      - config.env
secretGenerator:
  - name: notify-secrets
    envs:
      - secret.env
generatorOptions:
  disableNameSuffixHash: true
images:
  - name: ride-hailing/notify
    newTag: prod
