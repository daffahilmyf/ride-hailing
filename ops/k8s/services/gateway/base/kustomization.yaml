apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ride-hailing

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: gateway-config
    files:
      - config.yaml=../../../../services/gateway/config/config.yaml
