apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ride-hailing

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: matching-config
    files:
      - config.yaml=../../../../services/matching/config/config.yaml
